spec:
  volumes:
  - name: home
    projected:
      sources:
      - secret:
          name: maven-settings
          items:
            - key: settings-security.xml
              path: ".m2/settings-security.xml"
      - configMap:
          name: maven-settings
          items:
            - key: settings.xml
              path: ".m2/settings.xml"
      - configMap:
          name: npm-config
  - name: cache
    hostPath:
      path: "/var/tmp/build-cache"
  - name: registry
    secret:
      secretName: registry-cred
      items:
      - key: .dockerconfigjson
        path: "config.json"
  imagePullSecrets:
  - name: registry-cred
  containers:
  - name: maven
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
    volumeMounts:
    - mountPath: "/home/build"
      name: home
    - mountPath: "/cache"
      name: cache
    image: registry.dev.yashkov.org/yashkov/maven:3.8.3.0
    imagePullPolicy: Always
    resources:
      requests:
        memory: 1024Mi
        cpu: 100m
      limits:
        memory: 1200Mi
        cpu: 500m
    env:
    - name: HOME
      value: "/home/build"
    - name: JAVA_TOOL_OPTIONS
      value: "-XX:MaxRAMPercentage=15 -Duser.home=/home/build"
    command:
    - "/bin/cat"
    tty: true
  - name: kaniko
    volumeMounts:
    - mountPath: "/kaniko/.docker"
      name: registry
    image: gcr.io/kaniko-project/executor:debug
    imagePullPolicy: Always
    resources:
      requests:
        cpu: 10m
        memory: 64Mi
      limits:
        cpu: 100m
        memory: 80Mi
    command:
    - "/busybox/cat"
    tty: true
